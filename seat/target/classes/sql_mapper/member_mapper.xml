<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Hall">
	<!-- 회원가입 쿼리 -->
	<insert id="memberJoin" parameterType="member">
		INSERT INTO MEMBER VALUES(#{id}, #{pw} ,#{name},#{age},#{gender},#{email})
	</insert>
	  <!-- 아이디 중복확인 -->
   <select id="idOverlap" parameterType="member"
      resultType="member">
      SELECT * FROM MEMBER WHERE ID=#{id}
   </select>
   <select id="memberloginPw" parameterType="member" resultType="member">
   select * from member where id=#{id}
   
   </select>
	<select id="memberlist" parameterType="member" resultType="member">
   select * from member where id!='admin'

   </select>
   <select id="memberinfo" parameterType="member" resultType="member">
   select * from member where id=#{id}

   </select>
   
   <delete id="memberdel" parameterType="member">
   delete from member where id=#{id}
   </delete>

</mapper>